 - create cross validation framework (or use library?)
 
 - run cross validation for knn, to optimise k
 - try changing the alogorithm method parameter in knn algorithm
 
 - see if adding new variables (heavily weighted) is better solution
 - try pca
 
 - find next method to try??
